// import Vue from 'vue'
import ConnectRouting from '@/components/ConnectRouting'

describe('ConnectRouting', () => {
  it(`valid component`, () => {
    expect(typeof ConnectRouting.created).to.equal('function')
  })
})
